/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.asos-media.com'], // Add your allowed image domains here
  },
};

export default nextConfig;
